org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# ABI selection lives in app/build.gradle.kts (defaultConfig.ndk.abiFilters:
# arm64-v8a for real devices + x86_64 for the AVD). Do NOT set
# android.injected.build.abi here — it is the IDE-deploy injection property;
# it silently overrides abiFilters and routes the APK into intermediates/
# instead of outputs/apk/.
