.venv/
__pycache__/
*.pyc
test-results/
.env
tests/config_local.py

# macOS
.DS_Store

# Build artifacts
dist/
build/
*.egg-info/
*.egg

# IDE / editor
.vscode/
.idea/
.claude/

# MobileWorld runtime: machine-local symlink to the sibling checkout
# (third_party/MobileWorld -> ../../MobileWorld). run_mobileworld.py resolves
# the MW runtime here, so the benchmark uses the dev's live editable source
# instead of the pinned-git venv snapshot. Not portable, so not committed.
third_party/

# Cache
.mypy_cache/
.pytest_cache/
traj_logs/
artifacts/
recordings/
eval_figs/
