## What this changes

## Checklist

- [ ] `uv run python -m unittest discover -s tests` passes
- [ ] `uv run python scripts/validate/validate_manifests.py` passes (if manifests touched)
- [ ] For a new/updated card: verified on a real device — list device, app
      version, and date below (goes into `provenance`)
- [ ] For SPEC changes: linked issue with rationale + version bump

## Verification notes

Device / app version / what you ran, anything fragile or that didn't work.
